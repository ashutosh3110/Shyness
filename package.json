{
  "name": "shyness-app",
  "version": "1.0.0",
  "description": "A web platform to help users overcome shyness through daily video uploads",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "shyness",
    "public-speaking",
    "video-upload",
    "streak-tracking"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
